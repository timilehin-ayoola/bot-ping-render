# Comma-separated list of URLs to ping (including this bot's own URL)
URLS=https://service1.onrender.com,https://service2.onrender.com,https://ping-bot.onrender.com

# Interval in minutes between pings (default: 10)
INTERVAL_MINUTES=10

# Port for the Express server (Render sets this automatically)
PORT=3000
